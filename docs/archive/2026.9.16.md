# 2026.9.16：整理遗留代码与入口

## 背景

用户在只读检查后要求整理旧代码。当前目录无 Git 元数据；修改前先备份原文件与 SHA-256，路径为 `build/cleanup-2026-09-16/`。

## 改动

- `teleop.py`：删除无调用的 `sync_code_legacy` 和 `check_prerequisites_legacy`，保留 rsync/scp 双路径；更新真机入口并排除旧打包产物同步。
- `scripts/hardware/teleop_realman_rm65_safe_hardware.py`：承接推荐真机入口完整实现；旧 placo 名称脚本转为兼容包装。
- `xrobotoolkit_teleop/hardware/legacy/`：集中保存旧 Cartesian 控制器和旧单文件诊断实现；旧路径保留类/CLI 兼容包装，旧单文件禁止真机运动的检查保留。
- `tests/test_realman_mapping.py`、`tests/test_realman_safety.py`：更新历史实现的导入位置，补充旧/新入口兼容检查。
- `tests/test_teleop_launcher.py`：验证新真机入口、rsync/scp 同步路径。
- `config/realman_rm65_side_mount_teleop.json`：参考矩阵和 invert_tcp_xy 与当前源码对齐，注明未自动加载。
- `README.md`、`CLAUDE.md`、`docs/README.md`、`docs/VR_TELEOP_HANDOFF.md`、`docs/legacy-code.md`：更新推荐命令、默认值、历史实现和生成物保留说明；交接文档修正官方 IK seed 的现有语义。
- `pyproject.toml`：标注历史可选依赖组，保留外部安装兼容；`.gitignore`、`.dockerignore`：排除临时打包产物。
- 根目录旧清单及异常文件归档，原文件名与哈希另存；历史测试基线、打包产物和安装元数据保留。

## 行为边界

本次没有改变真机或仿真的运动参数、坐标变换、位置姿态同步跟随、连续 IK、限幅、Grip 停止和故障恢复逻辑。仿真侧检查入口和完整回归；不因入口整理修改仿真算法。兼容范围见维护说明。

## 验证

- 整理前：本地 `python -m pytest tests -q`，294 passed。
- 整理后：本地 `python -m pytest tests -q`，301 passed（增加 7 个回归用例），最终运行耗时 0.78 s。
- `python -m compileall -q teleop.py scripts xrobotoolkit_teleop tests` 通过；新入口/兼容包装及相关测试的 Black 检查通过（target-version py310）。
- AST 对照通过：新真机入口与原入口代码结构一致；迁移后的历史控制器仅新增说明，旧诊断仅新增说明和更新错误提示中的入口路径。
- 备份哈希、3 个归档文件哈希、配置矩阵/默认值、包元数据一致性检查全部通过，详见 `build/cleanup-2026-09-16/verification.json`。
- 未连接机械臂，未同步远程容器；本机缺少 tyro、Meshcat、XR SDK，CLI 转发使用替身验证，不等同于完整运行环境验收。
